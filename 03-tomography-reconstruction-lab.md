# 03. 보이지 않는 내부 복원소

- 프로젝트: `tomography-reconstruction-lab`
- 작성일: 2026-09-15 / 상태: 구현 전 설계 / 우선순위: 3
- 대상: 중학교 심화·고등학교 수학·과학·정보 / 활동: 25분
- [공통 원칙](./00-shared-design-principles.md) · [목록](./README.md)

## 1. 질문·차별성·우선순위

‘한 방향에서 같은 그림자가 생기는 물체는 내부도 같을까?’ 제한된 투영 측정으로 내부 밀도 분포를 복원한다.
기존 차원 단면 박물관은 알려진 입체를 자르지만, 이 앱은 모르는 내부를 측정값으로 추정한다.
지층 추론과도 달리 선적분과 영상 재구성이 핵심이다. 계산과 오차를 검증 가능한 작은 격자로 시작할 수 있어 3순위다.

## 2. 목표와 오개념

- 다른 내부가 같은 한 방향 투영을 만들 수 있음을 설명한다.
- 측정 방향·횟수·잡음이 복원에 미치는 영향을 비교한다.
- 복원 이미지를 실제 내부의 완벽한 사진으로 해석하지 않는다.
- 밝은 부분을 곧바로 특정 물질로 판정하지 않는다. P0 값은 무차원 모형 밀도다.

## 3. 25분 흐름

1. 3분: 같은 열 합을 가진 두 8×8 배열을 보고 구분 방법을 예측한다.
2. 5분: 0°, 90° 투영을 읽고 내부 후보를 선택한다.
3. 5분: 예산 12회 안에서 추가 각도를 선택한다.
4. 7분: 단순 역투영과 반복 재구성을 비교하고 잔차를 살핀다.
5. 5분: 정답 내부를 공개해 남은 오차를 설명한다.
- 전이: 같은 12회 측정을 비슷한 각도에 몰아 사용한 경우와 고르게 분산한 경우를 비교한다.

## 4. 화면과 상태

| 화면 | 내용 | 행동 |
|---|---|---|
| 밀봉 상자 | 외관과 가설 | 예측 기록 |
| 스캔 | 각도, 검출기 값, 남은 횟수 | 각도 입력·스캔 |
| 복원 | 내부 추정, 측정 잔차 | 반복 계산·취소 |
| 비교 | 각도 목록, 오차, 후보 | 실행 비교 |
| 공개 | 정답·추정·차이 지도 | 근거 기록 |

idle → measuring → measured → reconstructing → review. 스캔 완료 시에만 예산을 소비한다.
동일 각도 반복은 잡음 평균 실험에서는 허용하고 새 각도와 다른 의미임을 표시한다.
계산 취소 시 마지막 완료 반복 결과를 보존한다. 정답 공개 이후에는 해당 미션을 평가용으로 재사용하지 않는다.

## 5. 모델과 수치 정의

- 영역 [-1,1]×[-1,1] 모형 길이 단위, 32×32 픽셀, 밀도 x_j∈[0,1].
- 검출기 48개, 투영 각도 θ∈[0,180)°, 미션당 2~24회.
- y_i=Σ A_ij x_j+ε_i. A_ij는 i번째 광선이 j번째 픽셀 안에서 지나간 길이.
- P0는 로그 변환 후의 이상화된 선적분 데이터다. 실제 광자 감쇠·산란·검출기 보정은 제외한다.
- ε는 seed로 생성하는 평균 0의 가우스 잡음이며 σ=0,0.01,0.03의 모형값을 사용한다.
- 잡음으로 음수가 생겨도 원자료는 보존한다. 화면 색 범위와 데이터 범위를 혼동하지 않는다.
- 설명용 단순 역투영: A^T y를 누적해 흐릿한 결과를 보여준다. 정량 복원값과 동일시하지 않는다.
- P0 반복 복원: 최소화 0.5||Ax-y||²+0.5λ||x||², 0≤x≤1. 투영 경사하강법을 사용한다.
- 경사 A^T(Ax-y)+λx, step≤1/(||A||²+λ). 연산자 노름은 power iteration으로 추정 후 안전 여유를 둔다.
- 최대 200회, 상대 목적함수 변화 기준으로 조기 종료. λ=0.01은 초기 후보이며 고정 예제로 검증해 확정한다.
- 측정 오차와 영상 오차는 다르다. 작은 잔차만으로 정답 내부가 복원되었다고 판정하지 않는다.

## 6. 데이터·평가

- Phantom: id, version, gridSize, densities, shapes, seed. 정답은 학생 화면에 공개 시점 전 숨긴다.
- Scan: angleDeg, detectorCoordinates, values, noiseSigma, seed.
- Reconstruction: method, iterations, regularization, residualNorm, image.
- 공개 후 RMSE=√mean((estimate-truth)²), 공개 전에는 잔차와 측정 계획만 표시한다.
- 클라이언트 앱에서 정답을 보안적으로 숨긴다고 보장하지 않는다. 학습상 공개 순서만 제어한다.
- 평가: 서로 다른 방향의 정보 필요성, 예산 선택 근거, 복원 불확실성 설명.

## 7. 시각화

P0는 Canvas 밀도 지도와 SVG 광선·검출기 표를 사용한다. Three.js는 P1에서 여러 단층을 쌓아 입체로 보여준다.
P0에서 외관만 3D로 표시하더라도 복원 모델이 2D임을 명시한다.
시각화 실패 시 검출기 수치 표와 작은 격자 색상 표로 각도·측정 비교를 유지한다.

## 8. 생성 이미지

- P0 밀봉 상자·운석·기계 부품 외관 3장, 도입 카드 3장. 확장 12사건 × 외관 2장=24장.
- 예시: ‘밝은 교육용 연구실의 밀봉된 정육면체 상자, 정면과 측면이 보이는 일정한 시점, 내부 투시나 문자 없음’.
- 내부 정답, 투영 영상, 차이 지도는 엔진으로 만든다. 외관이 내부 정답을 암시하지 않게 한다.
- 실제 의료 영상으로 표현하지 않으며 사람 신체를 대상으로 하지 않는다.

## 9. 범위·구현 순서

- P0: 원·직사각형 팬텀, 32×32, 각도 선택, 두 복원 방식, 예산, 공개 후 차이.
- P1: 64×64, 정규화 비교, 단층 3D 적층. 필터 역투영은 별도 검증 후 도입한다.
- 제외: 임상 진단, 실제 장비 제어, 임의 사진의 진짜 내부 추정.
- M1: 광선-격자 길이 엔진 → M2: 전방/수반 연산 검증 → M3: 반복 복원 → M4: 학습 흐름 → M5: 시각·성능 검증.
- 모듈: rayGrid.ts, projector.ts, reconstruction.ts, phantom.ts, scanWorker.ts, ScanView.tsx, RevealView.tsx. 각각 500줄 미만.

## 10. 검증과 완료

- 영 밀도·무잡음의 모든 투영은 0. 균일 밀도 1인 영역을 수평 통과하는 광선 적분은 2.
- 90° 회전한 팬텀과 투영 각도 이동 관계를 검증한다.
- 수반 검사: 임의 x,y에 대해 <Ax,y>와 <x,A^Ty> 상대차 1e-8 이하 목표.
- 무잡음 작은 팬텀에서 목적함수가 증가하면 step 규칙을 재검토한다. 복원 오차를 숨기지 않는다.
- 각도 0/179.9°, 접선 광선, 빈 스캔, 중복 각도, 취소·재개를 검증한다.
- 모바일·키보드 각도 입력·reduced-motion·표 대체 화면에서 완료 가능해야 한다.
- gi-pulse는 스캔/비교 단계에만, 업데이트 내역은 공통 형식. VoiceOver 제외.
- 완료: 동일 seed의 스캔 재현, 실제 반복 계산, 정답 공개 전후 지표 구분, 예산 차감의 일관성.

## 11. 참고 자료와 한계

[NIST CT 기하 오차·재구성 연구](https://nvlpubs.nist.gov/nistpubs/jres/124/jres.124.014.pdf)는 투영과 재구성의 관계를 확인할 참고 자료다.
앞선 아이디어 조사에서 확인한 링크이며 본 문서의 단순화 모델에 대한 전문 검증을 대신하지 않는다.
