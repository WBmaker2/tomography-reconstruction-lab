# 03. 품질 패스 — UI 검증 기록

작성일: 2026-09-15. 대상: P0 빌드(에셋·λ=0.001 포함). 브라우저 실측 기준.

## 항목별 결과

| 항목 | 방법 | 결과 |
|---|---|---|
| 에셋 표시 | 외관 img naturalWidth, 도입 카드 3종 | 180px · 3/3 정상 |
| 시작 가드 | 예측 없이 시작 클릭 | 오류 안내 표시 후 단계 유지 |
| 키보드 | 다이얼 포커스 후 ArrowRight | 30.0° → 31.0° |
| 중복 각도 | 같은 각도 재스캔(평균 모드 OFF) | 안내와 함께 차단, 예산 미차감 |
| 반복 재구성 | 2각 측정 후 200회 실행 wall time | 841ms(2번째 스캔 포함), 잔차 0.0750 |
| 메모리 | 전이 과제 전후 JS 힙 | 1.4MB |
| reduced-motion | emulate reduce 후 펄스 버튼 계산 스타일 | animation none, 3px 황토 정적 테두리 |
| 가로 넘침 | 1280/768/360/320px scrollWidth | 1268/756/360/320 — 모두 viewport 이하 |
| RMSE 분리 | 공개 전 `RMSE =` 값 존재 여부 | 없음. 공개 후에만 표시 |
| 캡처 | 데스크톱 5화면 + 모바일 2화면 | 육안 확인済 (`.impeccable/review/`) |

## 수정으로 확정된 것

- 다이얼 숫자 30도 간격, 검출기 캡션 y=234·광선 클립, 부등호 말로 교체,
  잔차 상식 전역 테두리. детектор clean.

## 미검증 (정직한 기록)

- Canvas 2D 완전 실패 시 표 대체 화면: 코드 경로 존재, 브라우저 강제 실패 재현은 못 함.
  배포 후 수동 확인 항목으로 이관(§4).
- VoiceOver: 범위 제외(원칙 고지대로 커버리지 주장 안 함).
- 저장소 불가 폴백: 코드 경로 존재, 브라우저 차단 재현은 못 함. JSON 내보내기는 동작 확인済.
